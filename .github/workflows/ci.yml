name: CI Prep

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run CI sanity step
        run: python ci/run_ci_step.py

      - name: Run tests
        run: python ci/run_test.py
